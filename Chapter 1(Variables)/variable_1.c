#include <stdio.h> // Includes standard input/output functions like printf
int main() { // Entry point of the program

// Variables are containers which store a value .

int a; //Variable declaration
a = 14; //Variable initialization
//%d is the format specifier which tells the print to print an integer..
printf("This is a variable whose value is %d",a);
    return 0;  //It means that we are saying to the machine to stop the program as it executes here
}